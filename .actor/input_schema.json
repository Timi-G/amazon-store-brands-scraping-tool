{
  "title": "Amazon Store Brands Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "brands": {
      "title": "Brand Configurations",
      "type": "array",
      "description": "Add Amazon store brands to scrape. For each brand, specify which product data fields you want to collect.",
      "prefill": [
        {
          "brandName": "Swatch",
          "scrapeAsin": true,
          "scrapePage": true,
          "scrapeImageUrl": true
        }
      ],
      "editor": "schemaBased",
      "minItems": 1,
      "items": {
        "type": "object",
        "properties": {
          "brandName": {
            "title": "Brand Name",
            "type": "string",
            "description": "Enter the exact Amazon store brand name",
            "editor": "textfield"
          },
          "scrapeAsin": {
            "title": "Scrape ASIN",
            "type": "boolean",
            "description": "Collect product ASIN (Amazon Standard Identification Number)",
            "default": true,
            "editor": "checkbox"
          },
          "scrapePage": {
            "title": "Scrape Product Page URL",
            "type": "boolean",
            "description": "Collect product page URLs",
            "default": true,
            "editor": "checkbox"
          },
          "scrapeImageUrl": {
            "title": "Scrape Image URL",
            "type": "boolean",
            "description": "Collect product image URLs",
            "default": true,
            "editor": "checkbox"
          }
        },
        "required": ["brandName"],
        "additionalProperties": false
      }
    },
    "maxProductsPerBrand": {
      "title": "Maximum Products per Brand",
      "type": "integer",
      "description": "Limit the number of products scraped for each brand (leave empty for unlimited)",
      "editor": "number",
      "minimum": 1,
      "maximum": 10000,
      "unit": "products"
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Configure proxy settings. Using Apify Proxy with residential IPs is highly recommended for Amazon scraping to avoid blocks.",
      "prefill": { 
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"]
      },
      "editor": "proxy"
    },
    "debugMode": {
      "title": "Debug Mode",
      "type": "boolean",
      "description": "Enable detailed logging for troubleshooting and monitoring",
      "default": false,
      "editor": "checkbox",
      "groupCaption": "Advanced Options",
      "groupDescription": "Fine-tune scraping behavior and error handling"
    },
    "maxRetries": {
      "title": "Maximum Retries",
      "type": "integer",
      "description": "Number of retry attempts for failed requests",
      "default": 3,
      "minimum": 0,
      "maximum": 10,
      "editor": "number",
      "unit": "attempts"
    },
    "requestTimeout": {
      "title": "Request Timeout",
      "type": "integer",
      "description": "Maximum time to wait for each page to load",
      "default": 30,
      "minimum": 10,
      "maximum": 300,
      "unit": "seconds",
      "editor": "number"
    },
    "outputFormat": {
      "title": "Output Format",
      "type": "string",
      "description": "Choose how to organize the scraped data in the output",
      "default": "grouped",
      "editor": "select",
      "enum": ["grouped", "flat"],
      "enumTitles": ["Grouped by Brand", "Flat List"]
    }
  },
  "required": ["brands"]
}
